# KRX 지표 수집 코드 리뷰

## 구조 및 유지보수 문제
- **전역 상수 남용 및 하드코딩**: 로그/출력 경로가 `E:\python` 으로 고정돼 있어 다른 OS나 실행 위치에서 실패 가능성이 높습니다. 모든 설정이 모듈 전역에 흩어져 있어 재사용·테스트가 어렵습니다.
- **세션/재시도 관리의 불확실성**: `requests_cache`를 조건부로 덮어쓰면서도 기존 어댑터 설정을 항상 유지하지 않아, 연결 풀·재시도 설정이 일관되지 않을 수 있습니다. 글로벌 타임아웃과 per-request 타임아웃이 혼재돼 예측이 어렵습니다.
- **파싱 로직 중복 및 결합도**: HTML 파싱, 시계열 수집, 진행 표시 로직이 단일 파일에 뒤섞여 있어 단위 테스트가 어렵고 특정 기능만 재사용하기 힘듭니다.
- **예외 처리 및 종료 처리**: `os._exit`로 프로세스를 강제 종료하고, 신호 핸들러에서 바로 파일을 작성해 경쟁 상태가 발생할 수 있습니다. 예외가 발생해도 호출자에게 정보를 충분히 전달하지 못하고, 로깅/반환값 규약이 명확하지 않습니다.
- **플랫폼 의존 로직**: Windows 전용 콘솔 핸들러, 하드코딩된 경로, 크롬/코덱 관련 설정 등이 리눅스 환경에서 불필요하게 실행되며, 이는 추가 예외 요인이 됩니다.

## 성능 및 안정성 문제
- **과도한 네트워크 의존성**: `pykrx`와 외부 HTML 파싱을 동시에 사용하면서 실패 시 복구 전략이 제한적입니다. 캐시가 없는 환경에서는 동일 요청이 반복될 수 있습니다.
- **진행 출력 경쟁**: 스레드들이 stdout을 공유하지만 락으로 보호되는 구간이 최소화돼 있어 일부 출력이 섞일 가능성이 있습니다. 또한 진행률 계산이 전체 종목 수 기준인지, 재시도 라운드 기준인지 명확하지 않습니다.
- **데이터 정렬/헤더 조합 복잡성**: 열 이름을 문자열 조합으로 직접 관리해 실수 여지가 크고, 엑셀/CSV 저장 로직이 데이터 정렬과 결합돼 있습니다.

## 개선 방향
- 설정을 데이터클래스로 묶어 인스턴스화하고, 기본값을 실행 환경에 맞춰 계산하도록 합니다.
- 세션/재시도/타임아웃을 일관되게 관리하는 SessionManager 클래스를 도입합니다.
- HTML 파싱과 시계열 수집을 각기 독립적인 함수/메서드로 분리해 테스트 가능성을 높입니다.
- graceful shutdown(부분 저장)을 위한 컨텍스트 관리자를 두고, 프로세스 강제 종료 대신 예외 전파/정리 단계를 명확히 합니다.
- 출력/로그 형식을 한 곳에서 정의하고, 진행률 계산 기준을 명확히 합니다.
