playwright>=1.49
PyYAML>=6.0
requests>=2.31

# Windows 에는 시간대 데이터베이스가 없다. 이게 없으면 Asia/Seoul 을 못 찾는다.
tzdata>=2024.1; sys_platform == "win32"

# 선택: 설치하면 로그인 정보를 OS 키체인에 보관합니다
# (없으면 .state/ 안 파일에 권한 600 으로 저장됩니다)
keyring>=24.0
