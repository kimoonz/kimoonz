#!/usr/bin/env bash
# 설정이 제대로 됐는지 점검 — 문제가 생기면 이것부터 실행하세요
cd "$(dirname "$0")" || exit 1
[ -x .venv/bin/python ] || { echo "[막힘] 아직 설치가 안 됐습니다. ./setup.sh 를 먼저 실행해 주세요."; exit 1; }
.venv/bin/python -m paradogo "$@" doctor
echo
echo "=========================================================="
echo " 위 내용을 그대로 복사해서 붙여넣어 주시면 됩니다."
echo "=========================================================="
